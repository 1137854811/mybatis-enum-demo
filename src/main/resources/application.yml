spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.17.0.2:3306/test?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mybatis/*.xml

logging:
  level:
    com.example.mybatisenumdemo.dao: debug